spring:
  config:
    activate:
      on-profile: prod
  h2:
    console:
      enabled: false
  jpa:
    open-in-view: true

app:
  cookie:
    secure: true
    samesite: Lax
  security:
    allow-local-origins: false
    expose-docs: false
    expose-h2-console: false
